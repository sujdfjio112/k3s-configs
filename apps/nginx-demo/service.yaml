apiVersion: v1
kind: Service
metadata:
  name: nginx-demo-service
  namespace: default
spec:
  selector:
    app: nginx-demo
  ports:
    - protocol: TCP
      port: 8080        # cluster 内部访问端口
      targetPort: 8080  # 容器端口
  type: ClusterIP
